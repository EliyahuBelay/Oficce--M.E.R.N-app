{
  "name": "office",
  "version": "1.0.0",
  "description": "office-mern-app",
  "main": "Server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "npm install && cd client npm install",
    "server": "node Server/server.js",
    "start-dev": "nodemon node Server/server.js",
    "client":"cd client && npm run start",
    "dev": "concurrently --kill-others-on-fails \"npm run server\" \"npm run client\"",
    "heroku-postbuild": "cd client && npm install --only=dev && npm run build"
  },
  "author": "eliyahu",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "concurrently": "^7.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.2.3",
    "nodemon": "^2.0.15",
    "passport": "^0.5.2",
    "passport-jwt": "^4.0.0",
    "path": "^0.12.7"
  }
}
